import { OverlayerDirective } from './overlayer.directive';

describe('OverlayerDirective', () => {
  it('should create an instance', () => {
    const directive = new OverlayerDirective();
    expect(directive).toBeTruthy();
  });
});
